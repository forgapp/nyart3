{
  "rules": {
    ".read": "auth != null && root.child('Users').child(auth.uid).child('Authorized').val() == true",
    ".write": "auth != null && root.child('Users').child(auth.uid).child('Authorized').val() == true"
  }
}
